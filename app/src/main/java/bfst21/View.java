package bfst21;

import javafx.stage.Stage;

public class View {
    public View(Model model, Stage stage) {
        stage.setTitle("Initial window");
        stage.setWidth(200);
        stage.setHeight(400);
        stage.show();
    }
}
